{% extends 'layouts/app.html.twig' %}

{% block title %}{{ pageTitle }} | PrintFlow{% endblock %}
{% block app_navigation_active %}catalog{% endblock %}
{% block app_breadcrumb %}Catálogo / Unidades de medida / {{ pageTitle }}{% endblock %}

{% block app_content %}
    <div class="pf-page">
        <div class="pf-page-header">
            <div>
                <h1 class="pf-page-header__title">{{ pageTitle }}</h1>
                <p class="pf-page-header__subtitle">
                    La unidad determina cómo se venderá y cotizará cada concepto comercial.
                </p>
            </div>
        </div>

        <div class="pf-card">
            {{ form_start(form, {attr: {class: 'pf-form', novalidate: 'novalidate'}}) }}
                <div class="row g-3">
                    <div class="col-md-4">
                        {{ form_row(form.code) }}
                    </div>

                    <div class="col-md-8">
                        {{ form_row(form.name) }}
                    </div>

                    <div class="col-md-4">
                        {{ form_row(form.displayOrder) }}
                    </div>
                </div>

                <div class="pf-form-actions mt-4">
                    <a
                        href="{{ path('admin_catalog_units_index') }}"
                        class="btn pf-button pf-button--secondary"
                    >
                        Cancelar
                    </a>

                    <button type="submit" class="btn pf-button pf-button--primary">
                        <i class="bi bi-check-lg" aria-hidden="true"></i>
                        <span>{{ unit ? 'Guardar cambios' : 'Crear unidad' }}</span>
                    </button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}